<?php
$url = $this->url(
    array(
        'module'     => 'backend',
        'controller' => 'group',
        'action'     => 'create',
    ),
    'modules_i18n',True);
echo '<h2>'.$this->message.'</h2>';

$this->form->setAction($url)
    ->output();

if($this->dump)
{
    GC_Debug::dump($this->dump,true);
}