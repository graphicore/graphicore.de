<?php
$url = $this->url(
    array(
        'module'     => 'backend',
        'controller' => 'user',
        'action'     => 'login',
    ),
    'modules_i18n',True);
if(isset($this->message))
{
    echo '<h2>'.$this->message.'</h2>';
}
$this->form->setAction($url)
    ->output();

if($this->dump)
{
    GC_Debug::dump($this->dump,true);
}